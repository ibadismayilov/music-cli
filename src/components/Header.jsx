import React from 'react';
import { Box, Text } from 'ink';

const Header = () => (
    <Box marginBottom={1} justifyContent="center" borderStyle="double" borderColor="magenta">
        <Text color="cyan" bold> 🎵 MUSIC CLI </Text>
    </Box>
);

export default Header;

